📈 AI Global Stock Analyst Bot
บอทอัจฉริยะสำหรับติดตามข่าวสารหุ้นต่างประเทศ (Global Stocks) ทำหน้าที่เป็นผู้ช่วยนักวิเคราะห์ส่วนตัว โดยใช้ Google Gemini อ่านข่าวและประเมินผลกระทบต่อราคาหุ้น (Impact Score) ก่อนแจ้งเตือนเข้าสู่ LINE Group เฉพาะเรื่องที่สำคัญจริงๆ

✨ ฟีเจอร์เด่น (Features)
Multi-Ticker Support: รองรับการดึงข้อมูลหุ้นหลายตัวพร้อมกันผ่านไฟล์ target_ticker.txt

AI-Powered Analysis: ใช้ Gemini 2.0 / 2.5 วิเคราะห์ข่าว สรุปใจความภาษาไทย และให้คะแนนความรุนแรง (1-10)

Smart Filtering: แจ้งเตือนเฉพาะข่าวที่มีคะแนนผลกระทบสูงกว่าเกณฑ์ที่กำหนด (ลด Spam)

LINE Group Push: ส่งข้อความเข้ากลุ่ม LINE ผ่าน Messaging API (รองรับ Group ID)

Model Fail-over: มีระบบสลับโมเดล AI อัตโนมัติ (Flash <-> Pro) หากโมเดลหลักมีปัญหา

Rate Limiting: มีระบบหน่วงเวลาอัตโนมัติเพื่อป้องกัน API โควต้าเต็ม (สำหรับ Alpha Vantage Free Tier)

🛠️ Tech Stack
Language: Python 3.10+

Financial API: Alpha Vantage (News Sentiment)

AI Engine: Google Gemini API

Notification: LINE Messaging API

🚀 การติดตั้ง (Installation)
1. เตรียมไฟล์โปรเจกต์
ตรวจสอบว่าในโฟลเดอร์มีไฟล์ครบดังนี้:

main.py

test_main.py

requirements.txt

target_ticker.txt

.env

2. ติดตั้ง Libraries
รันคำสั่งใน Terminal:

Bash

pip install -r requirements.txt
3. ตั้งค่า API Keys (.env)
สร้างไฟล์ .env และใส่ค่า Configuration ดังนี้ (ห้ามมีช่องว่างหน้า/หลังเครื่องหมาย =):

ข้อมูลโค้ด

# 1. Alpha Vantage (ขอฟรีที่ alphavantage.co)
ALPHA_VANTAGE_API_KEY=ใส่คีย์ของคุณ

# 2. Google Gemini (ขอฟรีที่ aistudio.google.com)
GEMINI_API_KEY=ใส่คีย์ของคุณ

# 3. LINE Messaging API (developers.line.biz)
LINE_CHANNEL_ACCESS_TOKEN=ใส่_Token_ยาวๆ_ที่ได้จาก_Channel_Access_Token
LINE_GROUP_ID=Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
⚠️ หมายเหตุ: LINE_GROUP_ID ต้องขึ้นต้นด้วย C (Group) หรือ R (Room) เท่านั้น ห้ามใช้ User ID (U...) หากต้องการส่งเข้ากลุ่ม

4. กำหนดรายชื่อหุ้น
แก้ไขไฟล์ target_ticker.txt ใส่ชื่อหุ้นที่ต้องการ (บรรทัดละ 1 ชื่อ):

Plaintext

TSLA
NVDA
AAPL
MSFT
🖥️ การใช้งาน (Usage)
การรันโปรแกรมหลัก
สั่งให้บอทเริ่มทำงาน:

Bash

python main.py
ระบบจะอ่านชื่อหุ้นทีละตัว -> ดึงข่าว -> วิเคราะห์ -> ส่งไลน์ (ถ้าคะแนนถึงเกณฑ์) -> และพัก 15 วินาทีก่อนทำตัวถัดไป

การทดสอบระบบ (Testing)
รัน Unit Test เพื่อตรวจสอบความถูกต้องของโค้ดและการเชื่อมต่อ (โดยไม่เสียโควต้า API):

Bash

python test_main.py
⚙️ การปรับแต่ง (Configuration)
คุณสามารถปรับแก้ค่าคงที่ในไฟล์ main.py ได้:

IMPACT_THRESHOLD = 5: คะแนนขั้นต่ำที่จะให้ส่งไลน์ (1-10) ถ้าอยากให้ส่งยากขึ้นให้เพิ่มเลขนี้

models_to_try: ลำดับโมเดล Gemini ที่ต้องการใช้งาน

⚠️ ปัญหาที่พบบ่อย (Troubleshooting)
Q: ส่ง LINE ไม่ได้ (Error 400 Invalid ID)

A: เช็คไฟล์ .env ว่า LINE_GROUP_ID มีช่องว่างหรือเครื่องหมายคำพูด "" ติดมาหรือไม่ และต้องเชิญบอทเข้ากลุ่ม LINE นั้นก่อน

Q: Alpha Vantage แจ้งเตือน Limit

A: บัญชีฟรีจำกัดการเรียกประมาณ 25 ครั้ง/วัน หรือ 5 ครั้ง/นาที หากมีหุ้นเยอะเกินไป ให้ลดจำนวนหุ้นใน target_ticker.txt หรือรันวันละครั้งพอ

Q: Gemini Error 404 Model not found

A: ลองรัน pip install --upgrade google-generativeai หรือเช็คชื่อโมเดลในโค้ดว่าเป็นเวอร์ชันล่าสุดหรือไม่